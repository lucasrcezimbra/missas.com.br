# Engineering Conventions

## Django Ninja API colocation

Keep an endpoint and its request/response schemas in the same API module so the
endpoint signature can be understood without opening a separate schemas file.
